# Add at least one free key (more keys = more free usage, the router switches automatically)
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
# Optional: your own computer's models via Ollama (no limits)
# OLLAMA_URL=http://localhost:11434/v1
# OLLAMA_MODEL=qwen2.5:7b
# Images: free key from enter.pollinations.ai (sign in with GitHub; the "flux" model is free)
POLLINATIONS_TOKEN=
# Optional image backup: free Hugging Face token
# HF_TOKEN=
